
### Requirements Check
Before declaring done:
1. **Re-read the original prompt** — list every requirement. Consider the extended requirements from any architecture, design, or general planning you've done.
2. **For each requirement**, verify it's implemented using `winapp ui inspect` and `winapp ui invoke`
3. If anything is missing, go back to the top and implement everything
4. Only declare done when every requirement is checked and verified
