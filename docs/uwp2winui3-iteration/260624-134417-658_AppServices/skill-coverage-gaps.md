# Skill coverage gaps — `AppServices`

None. The single root cause (bootstrap copying `bin`/`obj` build output) is a
**skill-defect** (the copy step is wrong + the build-error class is undocumented), not a
prominence/clarity issue. Addressed in `skill-defects.json`.
