trigger:
  branches:
    include:
      - main
      - dev/**
  tags:
    exclude:
      - '*'
pr: none

resources:
  repositories:
  - repository: 1esPipelines
    type: git
    name: 1ESPipelineTemplates/1ESPipelineTemplates
    ref: refs/tags/release

extends:
  template: v1/1ES.Official.PipelineTemplate.yml@1esPipelines
  parameters:
    # SDL/compliance (PoliCheck, CredScan, Roslyn, BinSkim, etc.) is provided
    # by the 1ES template. Source analysis runs on this pool.
    sdl:
      policheck:
        enabled: true
      sourceAnalysisPool:
        name: Azure-Pipelines-1ESPT-ExDShared
        image: windows-latest
        os: windows
    stages:
    - stage: Build
      jobs:
      - job: Build
        pool:
          name: Azure-Pipelines-1ESPT-ExDShared
          image: windows-latest
          os: windows
          hostArchitecture: amd64
        steps:
          - checkout: self
          - task: UseDotNet@2
            displayName: Setup .NET 10
            inputs:
              version: 10.0.x
          # The 1ES pool blocks public api.nuget.org. Swap in a nuget.config
          # that points at the internal pde-oss_Internal upstream mirror feed.
          - script: move /Y $(Build.SourcesDirectory)\.pipelines\release-nuget.config $(Build.SourcesDirectory)\nuget.config
            displayName: Use internal NuGet feed
          - task: NuGetAuthenticate@1
          - template: ./.pipelines/templates/build.yaml@self
